# 复制为 model-local.yml 只用于本地接入提示；真实文件已被 Git 忽略。
# S1 不读取或调用模型。S3 起 API Key 必须使用数据库密文和外置主密钥，不写入本文件。
vibe:
  ai:
    provider:
      api-base: ${VIBE_MODEL_API_BASE:}
      model: ${VIBE_MODEL_NAME:}
      credential-configured: false
